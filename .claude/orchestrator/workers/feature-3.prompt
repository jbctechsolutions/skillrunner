You are a worker agent focused on implementing a single feature.

## Your Task
Feature 3: Wire run command (run.go) - Load skill from registry, create provider from config, execute workflow with WorkflowExecutor, display phase progress with spinner, show results (phases, tokens, timing), support --stream flag

## Orchestration Context
Wave 8: Workflow Engine Integration & CLI Wiring - Connect the existing infrastructure (workflow executor, session manager, storage repositories, skill loader) to CLI commands. Wire the stub CLI commands to actual services for a functional MVP.


## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE, create a file at: .claude/orchestrator/workers/feature-3.done
   with a brief summary of what you implemented
5. Do NOT commit your changes - the orchestrator will handle commits

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- NEVER commit, stage, or git add ANY of these files:
  - .claude/ (entire directory - orchestrator state, logs, prompts, worker files)
  - claude-progress.txt
  - init.sh
  - *.prompt, *.log, *.done, *.status files in .claude/


## Additional Context
Wire the run command to execute multi-phase workflows.

Key files to read first:
- internal/presentation/cli/commands/run.go (command to modify)
- internal/application/workflow/executor.go (workflow executor)
- internal/application/skills/registry.go (skill registry just created)
- internal/application/container.go (GetContainer, services)
- internal/presentation/cli/output/ (formatter, spinner)

Tasks:
1. Modify run.go runSkill() function:
   - Get Container from GetContainer()
   - Get SkillRegistry and load skill by name
   - Get WorkflowExecutor from container
   - Execute workflow: executor.Execute(ctx, skill, request)
   - Display phase progress using spinner
   - Show execution results (phases, tokens, timing, final output)

2. Support --stream flag for streaming output

3. Handle errors gracefully with user-friendly messages

4. Display execution summary with timing and token usage

The workflow executor already handles DAG execution, parallel phases, etc. You just need to wire it to the CLI.

Begin implementing the feature now.