You are a worker agent focused on implementing a single feature.

## Your Task
Feature 7: Create ProviderInitializer (internal/application/provider/initializer.go) - Set up providers from config, check health (Ollama connection, API keys). Modify status.go to display provider status and available models

## Orchestration Context
Wave 8: Workflow Engine Integration & CLI Wiring - Connect the existing infrastructure (workflow executor, session manager, storage repositories, skill loader) to CLI commands. Wire the stub CLI commands to actual services for a functional MVP.


## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE, create a file at: .claude/orchestrator/workers/feature-7.done
   with a brief summary of what you implemented
5. Do NOT commit your changes - the orchestrator will handle commits

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- NEVER commit, stage, or git add ANY of these files:
  - .claude/ (entire directory - orchestrator state, logs, prompts, worker files)
  - claude-progress.txt
  - init.sh
  - *.prompt, *.log, *.done, *.status files in .claude/


## Additional Context
Create the ProviderInitializer and wire provider status to the status command.

Key files to read first:
- internal/adapters/provider/registry.go (provider registry)
- internal/adapters/provider/ollama/ (Ollama adapter)
- internal/adapters/provider/anthropic/ (Anthropic adapter)
- internal/presentation/cli/commands/status.go (command to modify)
- internal/application/container.go (container initialization)

Tasks:
1. Create internal/application/provider/initializer.go with:
   - ProviderInitializer struct
   - InitFromConfig(*config.Config) to set up enabled providers
   - CheckHealth(ctx) to verify provider connectivity
   - GetAvailableModels() to list models per provider

2. Modify container.go to use ProviderInitializer in initRegistries()

3. Modify status.go:
   - Get provider registry from GetContainer()
   - Display actual provider status (enabled, healthy, models)
   - Show Ollama connection status if enabled
   - Show API key validity for cloud providers

Write tests for the initializer.

Begin implementing the feature now.