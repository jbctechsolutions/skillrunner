You are a worker agent focused on implementing a single feature.

## Your Task
Feature 5: Wire context commands (context_items.go, context_focus.go, context_checkpoint.go, context_rules.go) - Get repositories from container, implement add/list/remove for items, focus set/clear/show, checkpoint create/restore/list, rules management

## Orchestration Context
Wave 8: Workflow Engine Integration & CLI Wiring - Connect the existing infrastructure (workflow executor, session manager, storage repositories, skill loader) to CLI commands. Wire the stub CLI commands to actual services for a functional MVP.


## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE, create a file at: .claude/orchestrator/workers/feature-5.done
   with a brief summary of what you implemented
5. Do NOT commit your changes - the orchestrator will handle commits

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- NEVER commit, stage, or git add ANY of these files:
  - .claude/ (entire directory - orchestrator state, logs, prompts, worker files)
  - claude-progress.txt
  - init.sh
  - *.prompt, *.log, *.done, *.status files in .claude/


## Additional Context
Wire the context commands to their repositories.

Key files to read first:
- internal/presentation/cli/commands/context_items.go
- internal/presentation/cli/commands/context_focus.go
- internal/presentation/cli/commands/context_checkpoint.go
- internal/presentation/cli/commands/context_rules.go
- internal/application/container.go (repositories)
- internal/infrastructure/storage/context_item_repo.go
- internal/infrastructure/storage/checkpoint_repo.go
- internal/infrastructure/storage/rules_repo.go

Tasks:
1. context items add/list/remove:
   - Get ContextItemRepository from GetContainer()
   - Implement CRUD operations

2. context focus set/clear/show:
   - Track current focus in workspace or config

3. context checkpoint create/restore/list:
   - Get CheckpointRepository from GetContainer()
   - Create checkpoints with workspace state
   - Restore from checkpoint

4. context rules add/list/remove:
   - Get RulesRepository from GetContainer()
   - Manage context filtering rules

Display results in appropriate format (table/JSON).

Begin implementing the feature now.