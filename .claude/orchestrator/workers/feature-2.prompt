You are a worker agent focused on implementing a single feature.

## Your Task
Feature 2: Create SkillRegistry (internal/application/skills/registry.go) - Cache loaded skills, load from ~/.skillrunner/skills and embedded skills. Modify list.go to replace getMockSkills() with actual skill loading

## Orchestration Context
Wave 8: Workflow Engine Integration & CLI Wiring - Connect the existing infrastructure (workflow executor, session manager, storage repositories, skill loader) to CLI commands. Wire the stub CLI commands to actual services for a functional MVP.


## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE, create a file at: .claude/orchestrator/workers/feature-2.done
   with a brief summary of what you implemented
5. Do NOT commit your changes - the orchestrator will handle commits

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- NEVER commit, stage, or git add ANY of these files:
  - .claude/ (entire directory - orchestrator state, logs, prompts, worker files)
  - claude-progress.txt
  - init.sh
  - *.prompt, *.log, *.done, *.status files in .claude/


## Additional Context
Create the SkillRegistry for skill discovery and wire it to the list command.

Key files to read first:
- internal/infrastructure/skills/loader.go (existing skill loader)
- internal/presentation/cli/commands/list.go (command to modify)
- skills/*.yaml (built-in skill definitions)
- internal/application/container.go (container to get SkillLoader from)

Tasks:
1. Create internal/application/skills/registry.go with:
   - SkillRegistry struct caching loaded skills
   - LoadBuiltInSkills() to load from ./skills directory
   - LoadUserSkills(dir) to load from ~/.skillrunner/skills
   - GetSkill(name) to retrieve by name
   - ListSkills() to get all available skills

2. Modify list.go:
   - Get SkillLoader from GetContainer()
   - Replace getMockSkills() with actual skill loading
   - Load skills from both built-in and user directories
   - Display real skill metadata

3. Add Container method for SkillRegistry if needed

Write tests for the registry.

Begin implementing the feature now.