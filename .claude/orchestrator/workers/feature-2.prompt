You are a worker agent focused on implementing a single feature.

## Your Task
Create state storage port interfaces: WorkspaceStoragePort, SessionStoragePort, CheckpointStoragePort, ContextItemStoragePort, RuleStoragePort in internal/application/ports/state.go

## Orchestration Context
Wave 7: Storage Foundation - Implement SQLite infrastructure and state storage for Skillrunner MVP. This builds on the completed v2.0 implementation (Waves 1-6) which includes domain types, providers, routing, and CLI commands.


## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE, create a file at: .claude/orchestrator/workers/feature-2.done
   with a brief summary of what you implemented
5. Do NOT commit your changes - the orchestrator will handle commits

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- NEVER commit, stage, or git add ANY of these files:
  - .claude/ (entire directory - orchestrator state, logs, prompts, worker files)
  - claude-progress.txt
  - init.sh
  - *.prompt, *.log, *.done, *.status files in .claude/


## Additional Context
Create state storage port interfaces in internal/application/ports/state.go. Define:
1. WorkspaceStoragePort - CRUD for workspaces, GetByName, GetActive, SetFocus
2. SessionStoragePort - CRUD for sessions, GetByWorkspace, GetActive, UpdateStatus, UpdateTokenUsage
3. CheckpointStoragePort - CRUD for checkpoints, GetBySession, GetByWorkspace, GetLatest
4. ContextItemStoragePort - CRUD for items, GetByName, GetByTags, Search, UpdateLastUsed
5. RuleStoragePort - CRUD for rules, GetActive, GetByScope, Activate/Deactivate
6. Follow existing port patterns from internal/application/ports/provider.go
7. Use context.Context as first param, return domain errors
8. Include comprehensive Go doc comments

Begin implementing the feature now.