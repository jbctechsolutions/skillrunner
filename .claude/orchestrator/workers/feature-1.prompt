You are a worker agent focused on implementing a single feature.

## Your Task
Feature 1: Create application container (internal/application/container.go) with dependency injection - Initialize SQLite connection, create repository instances, SessionManager, WorkflowExecutor, SkillLoader. Wire container into AppContext in root.go

## Orchestration Context
Wave 8: Workflow Engine Integration & CLI Wiring - Connect the existing infrastructure (workflow executor, session manager, storage repositories, skill loader) to CLI commands. Wire the stub CLI commands to actual services for a functional MVP.


## Instructions
1. Focus ONLY on implementing this specific feature
2. Make small, incremental changes
3. Test your changes as you go
4. When you are DONE, create a file at: .claude/orchestrator/workers/feature-1.done
   with a brief summary of what you implemented
5. Do NOT commit your changes - the orchestrator will handle commits

## Important
- Do not work on other features
- If you encounter a blocker, document it in the .done file and stop
- Keep changes minimal and focused
- NEVER commit, stage, or git add ANY of these files:
  - .claude/ (entire directory - orchestrator state, logs, prompts, worker files)
  - claude-progress.txt
  - init.sh
  - *.prompt, *.log, *.done, *.status files in .claude/


## Additional Context
This is Wave 8 of Skillrunner v2.0. Waves 1-7 are complete with domain layer, application services, and storage adapters.

Your task: Create the application container for dependency injection.

Key files to read first:
- internal/presentation/cli/commands/root.go (AppContext struct and initializeApp)
- internal/infrastructure/storage/session_repo.go (example repository)
- internal/application/session/manager.go (SessionManager)
- internal/application/workflow/executor.go (WorkflowExecutor)
- internal/infrastructure/skills/loader.go (SkillLoader)
- internal/adapters/backend/registry.go (BackendRegistry)
- internal/adapters/provider/registry.go (ProviderRegistry)

Create:
1. internal/application/container.go with Container struct that holds:
   - DB connection (*sql.DB)
   - All repositories (Session, Workspace, Checkpoint, ContextItem, Rules)
   - SessionManager
   - WorkflowExecutor  
   - SkillLoader
   - ProviderRegistry
   - BackendRegistry

2. NewContainer(cfg *config.Config) function that:
   - Opens SQLite database at ~/.skillrunner/skillrunner.db
   - Creates all repository instances
   - Creates SessionManager with storage and backends
   - Creates WorkflowExecutor with provider
   - Creates SkillLoader

3. Modify root.go to:
   - Add Container field to AppContext
   - Initialize container in initializeApp()
   - Add GetContainer() helper function

Write unit tests for container initialization. Ensure existing tests still pass.

Begin implementing the feature now.